apiVersion: v1
kind: Service

metadata:
  name: onlineshop-service
  namespace: onlineshop

spec:
  selector:
    app: onlineshop

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080

  type: ClusterIP
